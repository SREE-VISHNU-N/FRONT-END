//Modules for to access the data of another file

/* const getnames = require('./data');

console.log(getnames.age,getnames.name
) */

const {name,age} = require('./data');

console.log('/n')

console.log(name,age);
